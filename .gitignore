# macOS / OS noise
.DS_Store
Thumbs.db
*.swp
*.swo

# Editor / IDE
.idea/
.vscode/

# Dependency directories (we vendor Three.js directly under `vendor/`, no npm install needed)
node_modules/

# Build artefacts (none currently, but harmless to have)
dist/
build/

# Secrets + generated config (tournament Supabase keys) — see SETUP-TOURNAMENT.md
.env.local
.env*.local
env.js

# Generated by scripts/build-i18n.mjs (source of truth: i18n/en.json + i18n/id.json)
i18n/strings.js
/id/

# Local server logs
*.log

# Temp files
*.tmp
.~lock.*
